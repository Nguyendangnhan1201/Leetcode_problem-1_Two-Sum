
def twoSum(nums: List[int], target: int):
    for i in nums:
            for j in range(nums.index(i)+1, len(nums)):
                sum=i+nums[j]
                if sum == target:
                    print([nums.index(i),j])
twoSum([1,2,3,4,5],6)